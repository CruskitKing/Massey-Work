CREATE TABLE IF NOT EXISTS notes (ID INTEGER PRIMARY KEY autoincrement, TITLE VARCHAR, DESCRIPTION VARCHAR, TSTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL);
INSERT INTO notes (TITLE, DESCRIPTION) VALUES ("Note Title", "Note Description");